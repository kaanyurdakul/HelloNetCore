<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
</head>
<body>
    <div>Menu</div>
    <div>
        @*@await  Component.InvokeAsync("StudentList", new { filter = "b" }) sending filter data without query string*@
        @await Component.InvokeAsync("StudentList")
    </div>
    <div>
        @RenderBody()
    </div>
    <div>Footer</div>
    <div>@RenderSection("MySection",false)</div>
</body>
</html>
